/**
 * UserController
 *
 * @description :: Server-side logic for managing users
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */

module.exports = {
  /**
   * `UserController.login()`
   */
  login: function (req,  res) {
    res.view(); 
  },

  /**
   * `UserController.new()`
   */  
  new: function (req,  res) {
    res.view(); 
  },

  /**
   * `UserController.harvest()`
   */  
  harvest: function (req,  res) {
    res.view(); 
  },

  /**
   * `UserController.favourites()`
   */  
  favourites: function (req,  res) {
    res.view(); 
  },

  /**
   * `UserController.backyard()`
   */  
  backyard: function (req,  res) {
    res.view(); 
  },

};

